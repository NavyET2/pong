var outputNumbers = [];


function pingPong(userNumber) {
  for (var list=1; list <= list; list +=1) {
    if(list %15 === 0) {
      outputNumbers.push("pingpong");
    } else if (list % 3 === 0) {
      outputNumbers.push("ping");
    } else if (list % 5 === 0) {
      outputNumbers.push("pong");
    } else {
      outputNumbers.push("index");
    }
  }
}
